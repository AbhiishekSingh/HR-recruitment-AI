# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pgvector==0.3.4

# Validation / settings
pydantic==2.9.2
pydantic-settings==2.5.2

# Task queue
celery==5.4.0
redis==5.0.8

# File parsing
pdfplumber==0.11.4
python-docx==1.1.2
# Renders scanned/image-only PDF pages to PNGs for GPT-5 vision extraction.
# Replaces pytesseract/pdf2image (removed) -- those needed the Tesseract and
# Poppler SYSTEM binaries installed separately (not pip-installable, and not
# present by default on Windows), which is what caused "couldn't read text"
# for any resume that wasn't a clean text-layer PDF. PyMuPDF ships its
# rendering engine inside the wheel itself, so `pip install` alone is enough
# on any OS.
pymupdf==1.24.11

# LLM / embeddings
openai==1.51.0
httpx==0.27.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
email-validator==2.2.0

# Utilities
tenacity==9.0.0
python-dotenv==1.0.1
